@using Restaurant.Models.ViewModels
@model InfoCart
<i class="fa-solid fa-cart-plus" style="color:brown"></i>
@if (Model.Count != 0)
{
    <span>Cart(@Model.Count Items / $ @Model.Amount)</span>
}
else
{
    <span>Cart(0 / $ 0)</span>
}